# DaggerHelpers

settings.gradle

```groovy
buildscript {
    repositories {
        jcenter()
        maven { url 'http://izumin.info/DaggerHelpers/repository' }
    }
    ...
}
```

build.gradle

```groovy
dependencies {
    compile 'info.izumin.android:daggerhelpers:0.0.1'
}
```